package assets;

import edu.usu.audio.SoundManager;

public class SoundAssets {
    private SoundManager audio;

    // sounds (need to be class specific)


    public SoundAssets() {
        audio = new SoundManager();
    }
}
